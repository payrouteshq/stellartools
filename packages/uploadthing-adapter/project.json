{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "uploadthing-adapter",
  "projectType": "library",
  "root": "packages/uploadthing-adapter",
  "sourceRoot": "packages/uploadthing-adapter/src",
  "tags": ["scope:integration", "publishable"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm run build",
        "cwd": "{projectRoot}"
      },
      "cache": true
    }
  }
}
