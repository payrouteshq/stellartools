{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "langchain-adapter",
  "projectType": "library",
  "root": "packages/langchain-adapter",
  "sourceRoot": "packages/langchain-adapter/src",
  "tags": ["scope:integration", "publishable"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm run build",
        "cwd": "{projectRoot}"
      },
      "cache": true
    }
  }
}
