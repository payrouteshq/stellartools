{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "betterauth-adapter",
  "projectType": "library",
  "root": "packages/betterauth-adapter",
  "sourceRoot": "packages/betterauth-adapter/src",
  "tags": ["scope:integration", "publishable"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm run build",
        "cwd": "{projectRoot}"
      },
      "cache": true
    }
  }
}
