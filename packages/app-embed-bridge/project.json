{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "app-embed-bridge",
  "projectType": "library",
  "root": "packages/app-embed-bridge",
  "sourceRoot": "packages/app-embed-bridge/src",
  "tags": ["scope:integration", "publishable"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm run build",
        "cwd": "{projectRoot}"
      },
      "cache": true
    }
  }
}
