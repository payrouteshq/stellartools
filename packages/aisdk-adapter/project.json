{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "aisdk-adapter",
  "projectType": "library",
  "root": "packages/aisdk-adapter",
  "sourceRoot": "packages/aisdk-adapter/src",
  "tags": ["scope:integration", "publishable"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm run build",
        "cwd": "{projectRoot}"
      },
      "cache": true
    }
  }
}
